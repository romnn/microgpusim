// /// A component in the cycle-driven simulation engine.
// ///
// /// Each cycle, the component's `cycle` method is invoked with the current simulation cycle.
// ///
// /// TODO: should return made progress?
// pub trait Component {
//     fn cycle(&mut self, cycle: u64);
//     // fn cycle(&mut self, cycle: crate::Cycle);
// }
